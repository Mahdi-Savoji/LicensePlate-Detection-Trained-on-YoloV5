# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: Dataset  # address of dataset root dir
train: Train_Data/images  # adress of train images (relative to 'path') 
val: Train_Data/images  # address of val images (relative to 'path') 
test:  Test_Data/images # address of test images (optional)

# Classes (1 Yolo classes)
names:
  0: Plate
